rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow open access for simplicity as requested by the user
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
