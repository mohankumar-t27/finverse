rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // During development, allow authenticated users to read/write anywhere
    // WARNING: This is insecure for production. 
    // We will replace this with stricter rules later.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
