# Preview Project Workflow

This document outlines the steps to start the development preview server locally.

## Start Development Server
```bash
cmd /c npm run dev
```

The app will be accessible at `http://localhost:9002`.
