# Build Project Workflow

This document outlines the steps to install dependencies, typecheck, and build the Next.js application.

## 1. Install Dependencies
```bash
cmd /c npm install
```

## 2. Typecheck Code
```bash
cmd /c npm run typecheck
```

## 3. Build Application
```bash
cmd /c npm run build
```
